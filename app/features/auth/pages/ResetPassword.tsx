import AuthPageWrapper from "../components/AuthPageWrapper";
import ResetPasswordForm from "../components/ResetPasswordForm";

const ResetPassword = () => {
  return <AuthPageWrapper content={<ResetPasswordForm />} />;
};

export default ResetPassword;
